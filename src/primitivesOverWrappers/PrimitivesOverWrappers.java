package primitivesOverWrappers;

import java.util.Comparator;

public class PrimitivesOverWrappers {
    static Integer i;
    public static void main(String[] args) {
//        long time=System.currentTimeMillis();
//        long sum=0L;
//        for (long i = 0; i < Integer.MAX_VALUE; i++) {
//            sum+=i;
//        }
//        System.out.println(sum);
//        System.out.println(System.currentTimeMillis()-time);

//        Comparator<Integer>naturalOrder=new Comparator<Integer>() {
//            @Override
//            public int compare(Integer first, Integer second) {
//               // return first<second ? -1 : (first == second ? 0 : 1); // wrong . need equals
//                return first<second ? -1 : (first.equals(second)  ? 0 : 1);
//            }
//        };
//        System.out.println(naturalOrder.compare(new Integer(15),new Integer(15)));
        if(i==15);
        System.out.println("Unbelievable");
    }
}
